# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(clientTaskLib)
ADD_LIBRARY(${CLIENT_TASKLIB_NAME} SHARED 
    ${CLIENT_TASKLIB_SOURCES})
add_dependencies(${CLIENT_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${CLIENT_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${CLIENT_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${CLIENT_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${CLIENT_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${CLIENT_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${CLIENT_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${CLIENT_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${CLIENT_TASKLIB_HEADERS}
    DESTINATION include/orocos/client)

